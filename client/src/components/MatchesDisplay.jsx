import { styled } from 'styled-components';

const MatchesDisplay = () => {
  return <Wrapper>MatchesDisplay</Wrapper>;
};

const Wrapper = styled.div`
  padding: 20px;
  height: 60vh;
  overflow-y: auto;
`;

export default MatchesDisplay;
