export const PATHS = {
  home: '/',
  dashboard: '/dashboard',
  onboardingUsers: '/onboarding',
};

export const PUBLIC_PATHS = [PATHS.home];

export const FORM_TYPES = {
  REGISTER: 'register',
  LOGIN: 'login',
};
