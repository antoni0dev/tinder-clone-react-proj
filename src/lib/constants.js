export const PATHS = {
  home: '/',
  dashboard: '/dashboard',
  onboardingUsers: '/onboarding-users',
};

export const PUBLIC_PATHS = [PATHS.home];

export const FORM_TYPES = {
  REGISTER: 'register',
  LOGIN: 'login',
};
